fn main() {
    let r: &i32;
    {
        let x: i32 = 1;
        r = &x;
        println!("{r}");
    }
    // println!("{r}");
}

// lifetime: 参照が有効であるスコープ(多くの場合推論される)
// -> ダングリングポインタを防ぐ目的で使用される
// 
// lifetimeはその参照元の生存期間に包含されている必要がある
